%div{id:"gauge-#{@query.id}", style: inline_dimension(@layout)}


:javascript
  var asInitVals = new Array();
  $(document).ready(function() {
    var g = new JustGage({
      id: "gauge-#{@query.id}",
      value: #{@data_to_present.data.first.to_a[0][1]},
      min: 0,
      max: #{@data_to_present.data.first.to_a[1][1]},
      title: "#{@query.name}"
    });
  });


